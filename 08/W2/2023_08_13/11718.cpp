#include <bits/stdc++.h>
#define FastIO ios_base :: sync_with_stdio(false); cin.tie(0); cout.tie(0);
using namespace std;

int main() {
    FastIO
    string input;
    while (getline(cin, input)) {
        cout << input << '\n';
    }
    return 0;
}