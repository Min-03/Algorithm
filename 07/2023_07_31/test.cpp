#include <bits/stdc++.h>
using namespace std;

pair<int, int> test() {
    pair<int, int> res(-1, -2);
    return res;
};
int main() {
    ios_base :: sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int num = test().first;
    int dist = test().second;
    cout << num << ' ' << dist;
    return 0;
}