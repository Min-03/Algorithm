#include <bits/stdc++.h>
#define FastIO ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
using namespace std;
const long long inf = 0x3f3f3f3f;

long long nums[100];

int main() {
    FastIO
    memset(nums, inf, sizeof(nums));
    for (int i = 0; i < 100; i++) cout << nums[i] << '\n';
    return 0;
}